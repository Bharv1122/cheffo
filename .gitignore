# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Claude Code local config (launch profiles, settings, hooks).
# settings.json is checked in on purpose: containers are cloned fresh, so a
# hook only runs if it is tracked. Personal overrides go in settings.local.json.
.claude/*
!.claude/settings.json
